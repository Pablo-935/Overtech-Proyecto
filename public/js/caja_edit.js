window.addEventListener("DOMContentLoaded", function() {

// let saldo_inicial_caja = document.getElementById("saldo_inicial_caja");
// let total_ingresos_caja = document.getElementById("total_ingresos_caja");
// let total_egresos_caja = document.getElementById("total_egresos_caja");
// let total_saldo_caja = document.getElementById("total_saldo_caja");


// saldo_inicial_caja.innerHTML = saldo_inicial_caja.value.toLocaleString("en-US", { minimumFractionDigits: 2, maximumFractionDigits: 2 });
// total_ingresos_caja.innerHTML = total_ingresos_caja.value.toLocaleString("en-US", { minimumFractionDigits: 2, maximumFractionDigits: 2 });
// total_egresos_caja.innerHTML = total_egresos_caja.value.toLocaleString("en-US", { minimumFractionDigits: 2, maximumFractionDigits: 2 });
// total_saldo_caja.innerHTML = total_saldo_caja.value.toLocaleString("en-US", { minimumFractionDigits: 2, maximumFractionDigits: 2 });

// let saldoInicialValor = parseFloat(saldo_inicial_caja.value);
// let totalIngresosValor = parseFloat(total_ingresos_caja.value);
// let totalEgresosValor = parseFloat(total_egresos_caja.value);
// let totalSaldoValor = parseFloat(total_saldo_caja.value);



});
